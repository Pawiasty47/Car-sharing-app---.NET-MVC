@model List<Chat>

@{
    ViewData["Title"] = "Twoje czaty";
}

<div class="container mt-4">
    <h3 class="mb-4 text-secondary">Twoje czaty</h3>

    @if (!Model.Any())
    {
        <div class="alert alert-light text-center py-5">
            <i class="bi bi-chat-dots fs-1 d-block mb-3"></i>
            Brak dostępnych czatów.
        </div>
    }
    else
    {
        <div class="row g-3">
            @foreach (var chat in Model)
            {
                <div class="col-md-6 col-lg-4">
                    <div class="card shadow-sm h-100">
                        <div class="card-body d-flex flex-column justify-content-between">
                            <div>
                                <h5 class="card-title mb-2 text-primary">
                                    @if (chat.Ride?.StartLocation != null && chat.Ride?.EndLocation != null)
                                    {
                                        @chat.Ride.StartLocation.City  @chat.Ride.EndLocation.City
                                            }
                                    else
                                    {
                                        <text>Przejazd</text>
                                    }
                                </h5>
                                <p class="text-muted mb-1 small">
                                    Kierowca: @chat.Ride?.Driver?.User?.FirstName @chat.Ride?.Driver?.User?.LastName
                                </p>
                                <p class="text-muted mb-0 small">
                                    Wiadomości: @(chat.Messages?.Count ?? 0)
                                </p>
                            </div>
                            <div class="mt-3 text-end">
                                <a asp-action="RideChat"
                                   asp-controller="Chat"
                                   asp-route-rideId="@chat.RideId"
                                   class="btn btn-sm btn-primary">
                                    Otwórz czat
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>